import { describe} from "mocha";
import { expect } from "chai";

describe('Sample unit test', function() {
    it('Should run unit tests', function() {
    });

    it('2 plus 2 should equal 4', function() {
        expect(2+2).to.equal(4);
    });
});
